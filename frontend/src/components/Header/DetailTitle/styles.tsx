import styled from '@emotion/styled';

const DetailTitleWrapper = styled.div`
  display: flex;
  align-items: center;
  gap: 1.2rem;
`;

const DetailTitleTextWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
`;

export { DetailTitleWrapper, DetailTitleTextWrapper };
